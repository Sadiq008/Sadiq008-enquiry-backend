<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enquiries List</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-700 text-white">
    <div class="container mx-auto p-6">
      <a
        href="http://localhost:5173/"
        class="text-blue-400 hover:underline mb-6 block"
        >Back to Home</a
      >
      <h1 class="text-center text-4xl font-bold mb-8">All Enquiries</h1>
      <div class="flex flex-col gap-6">
        <% if (users && users.length > 0) { %> <% users.forEach(function(user) {
        %>
        <div
          class="bg-gray-800 border border-gray-700 rounded-lg p-6 shadow-lg"
        >
          <h3 class="text-2xl font-semibold mb-2"><%= user.name %></h3>
          <p class="text-lg mb-4"><%= user.email %></p>
          <p class="text-lg mb-4"><%= user.message %></p>
          <div class="flex justify-end gap-4">
            <a
              class="text-white bg-yellow-500 hover:bg-yellow-400 border border-yellow-600 rounded-md px-4 py-2 transition duration-300"
              href="/edit/<%= user.id %>"
              >Update</a
            >
            <a
              class="text-white bg-red-600 hover:bg-red-500 border border-red-700 rounded-md px-4 py-2 transition duration-300 cursor-pointer"
              href="/delete/<%= user.id %>"
              >Delete User</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-gray-400 text-xl text-center">No enquiries yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
